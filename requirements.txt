# Direct dependencies only. Transitive packages are resolved by pip, and the
# lower bounds are what the code is known to work against.
#
# The previous pin set included torch/easyocr/opencv/scipy, none of which are
# imported anywhere in this project; torch==1.12.1 also has no wheels for
# current Python versions, which made `pip install -r requirements.txt` fail
# outright on Apple Silicon.

fastapi>=0.115
uvicorn>=0.34
playwright>=1.51
beautifulsoup4>=4.13
ollama>=0.4.7
pydantic>=2.11
